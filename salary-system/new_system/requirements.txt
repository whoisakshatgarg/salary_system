# New system runtime deps. The core (payroll engine, SQLite data layer, rules)
# uses ONLY the Python standard library — these are needed for the web API and
# the Excel exporters (next phase).
fastapi>=0.115
uvicorn[standard]>=0.30
openpyxl>=3.1          # exact-format CEO + Distribution salary sheets
python-multipart>=0.0.9
