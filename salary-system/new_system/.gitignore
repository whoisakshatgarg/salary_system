__pycache__/
*.pyc
# Everything the app writes at runtime is per-user state, never committed:
# the DB, backups, the session secret, inventory attachment files, ...
data/

# PyInstaller build output
build/
dist/
build-venv/
*.session_secret
